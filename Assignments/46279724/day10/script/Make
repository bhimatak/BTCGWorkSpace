C = gcc

INC = ../inc
SRC = ../src
BIN = ../bin
OBJ = ../obj

CFLAGES = -c -g -Wall
LDFLAGES = -O
IFLAGS = -I $(INC)

a.out:$(OBJ)/maxcount.o
 $(CC) $(LDFLAGS) $(BIN)/a.out $?
$(OBJ)/maxcount.o:$(SRC)/maxcount.c
 $(CC) $(CFLAGS) $(SRC)/maxcount.c $(IFLAGS)
 mv maxcount.o $(OBJ)/

clean:
          rm -f a.out *.o
          rm -f $(OBJ)/*
          rm -f(BIN)/*
          @echo "cleaning is done!"
